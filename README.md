# alpha_php_bootstrap
The noobie tries his hand at some php, json and bootstrap...

This simple single page makes use of php and bootstrap to randomly experiment features and parse username password from json file to validate login...

Uses php for updating session, parsing json and validating the login.
Bootstrap is used for allignment of the login form.
